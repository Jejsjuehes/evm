const config = {
  amount: [0.0002, 0.0001], // random amount ETH | số lượng ETH gửi tới các ví, random ngẫu nhiên
  number_transactions: 1, // number of transactions each wallet | số giao dịch thực hiện trên mỗi ví
};

module.exports = { config };
